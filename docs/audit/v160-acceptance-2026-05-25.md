# v1.6.0 acceptance — 2026-05-25

Combined release covering three streams. Cut from `feat/v1.6.0-dawn-and-sweep`.

## What shipped

### Stream A — MCP_LOCKDOWN role
- `src/auth.ts`: adds `'lockdown'` to the `Role` union; `resolveAuth` short-circuits when `MCP_LOCKDOWN=true` env var is set
- `src/env.ts`: `MCP_LOCKDOWN?: string` declared (optional, "true" activates)
- `src/index.ts`: `/health` exposes `lockdown: bool`
- `src/tools/index.ts`: `toolsForRole('lockdown')` filters `!t.isWrite && !t.adminOnly`
- `src/tools/__tests__/lockdown.test.ts`: 7 tests — also serves as `isWrite`-classification audit via `WRITE_NAME_PATTERNS` regex sweep

### Stream B — Dawn SMS tools
- `src/tools/dawn/identify_tech_by_phone.ts` + tests (4) — voice_registry → technicians two-tier lookup
- `src/tools/dawn/save_tech_debrief.ts` + tests (3) — idempotent D1 INSERT on `dawn_text_debriefs`
- `src/tools/index.ts`: Dawn import group registered
- `src/tools/__tests__/schemas.test.ts` + `src/tools/__tests__/coverage_gate.test.ts` + `src/routes/admin-endpoints.ts`: coverage exemption + tool-count updates (87 → 89; default visible 86 → 88)

### Stream C — readST sweep finished
- `src/st.ts`: new `readSTPost` helper (mirrors `readST` for POST-as-read endpoints)
- `src/tools/dispatch/get_capacity.ts`: migrated to `readSTPost`
- `src/tools/dispatch/st_get_capacity_slots.ts`: migrated to `readSTPost`
- `src/tools/reporting/st_run_report.ts`: `run`-mode POST migrated to `readSTPost`
- `src/tools/pricebook/search_pricebook_all.ts`: confirmed D1-only (not an ST live read); added 5 smoke tests
- README + CHANGELOG: removed stale "~25 hand-rolled fetch tools" language

## Test count

| Phase | Tests |
|---|---|
| v1.5.1 baseline | 451 |
| + Stream A lockdown | 458 |
| + Stream B Dawn tools | 465 |
| + Stream C readST sweep | 472 |

**472 passing, no regressions, no skips.** `npm run check` clean (typecheck + vitest).

## D1 migrations applied

`taylor-ai` migration `0033_dawn_text_debriefs.sql` — applied to both dev (`28848a36-3210-4584-830b-fb1508b7503b`) and prod (`ba02a8c6-bcff-4119-b797-0b4250a3edcf`). Verified empty (count=0). Migration committed to taylor-ai main as commit `a5cebee`.

## Linked spec + plan

- Spec: `qsc-infra/docs/superpowers/specs/2026-05-25-miss-dawn-text-agent-design.md`
- Plan: `qsc-infra/docs/superpowers/plans/2026-05-25-miss-dawn-text-agent.md`
- Pre-existing v1.6 plan: `docs/mcp/v1.6-PLAN.md` (committed in `8d65082`)

## Bake-off context

Dawn SMS now has two products in flight (Luke decision 2026-05-25):
1. **dawn-sms** (live, separate repo `lpeluso-dotcom/dawn-sms`) — Twilio MMS → ST job-attachment intake on toll-free **+1 (855) 695-5398**. No LLM, no Retell.
2. **dawn-text** (this release) — Retell text agent on **(843) 733-9568**. LLM-driven conversational debrief, calls these two new MCP tools.

Real techs decide which one earns its keep.

## Deferred to v1.6.1 / v1.7

- Response-shaper rollout to ~62 remaining hand-rolled tools (1-2 days).
- Filter-preservation harness coverage across ~80 tools (incremental).
- ST-77.2/77.3 probes (separable spikes).
- Tool-pack splitting (role/domain views).
