{
  "name": "mcp-servicetitan",
  "version": "1.2.0",
  "description": "QSC ServiceTitan MCP server — v1.2 (65 tools: 41 L4 purposeful + 10 L5 composites + 1 admin gateway + 3 Siro + 3 v1.2: capacity-slots, run-report, marketing-attribution). Streamable HTTP via Agents SDK; HMAC write-gate; D1 audit; Analytics Engine telemetry.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preflight": "bash scripts/preflight.sh",
    "preflight:dev": "bash scripts/preflight.sh --env dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.11.4",
    "hono": "^4.6.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250403.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.0.0"
  }
}
