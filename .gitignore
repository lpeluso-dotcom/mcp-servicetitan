node_modules
.npm-cache/
.worktrees/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
dist/
*.log
.DS_Store

# Generated ST probe artifacts — contain customer PII, never commit
scripts/probe-logs/
scripts/probe-snapshots/

# Local Claude Code config — untracked, must not be swept in by `git add -A`
.claude/

# Tool-selection eval run artifact (regenerated per `npm run eval:tools`)
scripts/evals/last-run.json
