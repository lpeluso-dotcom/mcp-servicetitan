node_modules
.npm-cache/
.worktrees/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
dist/
*.log
.DS_Store

# Generated ST probe artifacts — contain customer PII, never commit
scripts/probe-logs/
scripts/probe-snapshots/

# Local Claude Code config — untracked, must not be swept in by `git add -A`
.claude/

# Tool-selection eval run artifact (regenerated per `npm run eval:tools`)
scripts/evals/last-run.json

# Agent session artifact; contains real D1 database UUIDs in prose. Never commit
# to this PUBLIC repo (flagged in the 2026-08-23 Wave-1 plan risk table).
.phoenix-report.md
